apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockify-distillation.fullname" . }}
  labels:
    {{- include "blockify-distillation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blockify-distillation.selectorLabels" . | nindent 4 }}
